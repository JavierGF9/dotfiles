# ~/.xsessionrc: sourced by Xsession when logging in a desktop environment
# The only purpose of this file is to source ".profile" when logging in
# with LightDM in Debian.
# NOTE: probably should only source ".xprofile" instead of ".profile"

[ -r "$HOME/.xprofile" ] && . "$HOME/.xprofile"
[ -r "$HOME/.profile" ] && . "$HOME/.profile"
